config defaultToCurrentScreen true
config nudgePercentOf screenSize
config resizePercentOf screenSize
config secondsBetweenRepeat 0.1
config checkDefaultsOnLoad true
config focusCheckWidthMax 3000

# alias
alias sox screenOriginX
alias soy screenOriginY
alias ssx screenSizeX
alias ssy screenSizeY

# Resize Bindings
bind right:ctrl;alt  resize -10% +0 bottom-right
bind left:ctrl;alt   resize +10% +0 bottom-right
bind up:ctrl;alt     resize +0   +10% bottom-right
bind down:ctrl;alt   resize +0   -10% bottom-right
bind m:cmd;ctrl,alt  move 0;0 ${ssx}+20;${ssy}                                  # fullscreen
bind c:cmd;ctrl,alt  move ${ssx}/4;${ssy}/4 ${ssx}/2;${ssy}/2                      # center

# Push Bindings
bind left:ctrl;alt;cmd    move 0;0 ${ssx}/2;${ssy}                                 # left half
bind right:ctrl;alt;cmd   move ${ssx}/2+4;0 ${ssx}/2;${ssy}						   # right half 
bind up:ctrl;alt;cmd      move 0;0 ${ssx}+20;${ssy}/2                              # top half
bind down:ctrl;alt;cmd    move 0;${ssy}/2 ${ssx}+20;${ssy}/2                       # down half

# Throw Bindings
bind 1:ctrl;alt         throw 0 resize
bind 2:ctrl;alt         throw 1 resize
bind 3:ctrl;alt         throw 2 resize
bind right:ctrl;alt;cmd throw right resize
bind left:ctrl;alt;cmd  throw left  resize
bind up:ctrl;alt;cmd    throw up    resize
bind down:ctrl;alt;cmd  throw down  resize

# Focus Bindings

# Window Hints
bind esc:cmd hint
